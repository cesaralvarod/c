#include <stdio.h>

int main() {

    // used to replcae a statement where an operator takes a variable as one of
    // this arguments and then assigns the result back to the same variable

    int x = 10;

    x += 2;

    printf("%d", x);

    return 0;
}
